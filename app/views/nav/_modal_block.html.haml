.center_block.hide#north_big_field
  #overlay_background
    .outer_border#big_north_image
      .image_block
        .troika_headline
          Hubble Deep Field North
        = render partial: 'nav/close_x'
        #hdfn_big_image
.center_block.hide#south_big_field
  #overlay_background
    .outer_border#big_south_image
      .image_block
        .troika_headline
          Hubble Deep Field South
        = render partial: 'nav/close_x'
        #hdfs_big_image

.center_block.hide#wrong_sample_estimate
  #overlay_background
    .narrower_wrong_modal.position_modal
      %br
        .troika_headline
          Oops! 
      %p
        .std_text
          To find the smallest reasonable sample, look for the smallest size that keeps the natural variability of results about as low as could be obtained by sampling much more of the population.
      %p
        .std_text
          Here, the total number of galaxies in your HDF is slightly over 1,000.    But a sample of 40-50 adequately represents this population because it yields about as low a variability as a sample of almost 1,000.
      %p
        .std_text
          Try again or let us choose an appropriate sample size of 45 for you.

      .button_outside.close_modal_general.opps_try_adjust
        .button_inside
          .button_text
            TRY AGAIN           
      .button_outside#go_ahead
        .button_inside
          .button_text
            NEXT         
.center_block.hide#implausible_sorry
  #overlay_background
    .wrong_modal.position_modal.symmetry_modal
      %br
        .troika_h2
          Sorry!
        .short_break
        .std_text
          Sorry,
        = render :partial=>'nav/implausible'
.center_block.hide#implausible_yes
  #overlay_background
    .correct_modal.position_modal.symmetry_modal
      %br
        .troika_h2
          That's right
        .short_break
        .std_text
          That's right,   
        = render :partial=>'nav/implausible'
- if @warn  
  #overlay_background.field_selection_modal
    .wrong_modal.position_modal.enter_modal 
      %br
        .troika_h2
          HDF Field Selection
        %p
          .std_text
            You need to pick galaxies first 
        = render partial: 'nav/close_x'        